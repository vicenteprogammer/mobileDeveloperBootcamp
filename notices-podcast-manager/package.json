{
  "name": "notices-podcast-manager",
  "version": "1.0.0",
  "description": "Uma api que gerencia os principais podcast de notícias.",
  "main": "index.js",
  "scripts": {
    "start:dev": "tsx --env-file=.env src/server.ts",
    "start:watch": "tsx watch --env-file=.env  src/server.ts",
    "dist": "tsup src",
    "start": "npm run dist && node --env-file=.env dist/server.js"
  },
  "author": "Vicente Junior",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.0.15",
    "tsup": "^8.5.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
